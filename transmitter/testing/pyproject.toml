[project]
name = "testing"
version = "0.1.0"
description = "WCAN test analysis tools"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyserial",
    "pyyaml",
    "matplotlib",
]
